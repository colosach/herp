meta {
  name: Create a brand
  type: http
  seq: 1
}

post {
  url: {{baseURI}}inventory/brand
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:multipart-form {
  name: Hero
}

settings {
  encodeUrl: true
}
