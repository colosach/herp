meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{baseURI}}auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "ukandu121g358",
    "password": "password"
  }
}

tests {
  const body = res.body;
  bru.setEnvVar("token", body.data.token);
  
}

settings {
  encodeUrl: true
}
