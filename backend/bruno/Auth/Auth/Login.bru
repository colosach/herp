meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{baseURI}}auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "pos_staff1",
    "password": "pos123"
  }
}

tests {
  const data = res.body;
  bru.setEnvVar("token", data.token);
  
}

settings {
  encodeUrl: true
}
