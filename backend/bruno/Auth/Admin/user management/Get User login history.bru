meta {
  name: Get User login history
  type: http
  seq: 1
}

get {
  url: {{baseURI}}admin/login-history
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
}
