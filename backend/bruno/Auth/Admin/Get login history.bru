meta {
  name: Get login history
  type: http
  seq: 2
}

get {
  url: {{baseURI}}admin/login-history
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
}
