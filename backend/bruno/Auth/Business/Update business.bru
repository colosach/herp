meta {
  name: Update business
  type: http
  seq: 7
}

patch {
  url: {{baseURI}}business/2
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "website": "example.com"
  }
}

settings {
  encodeUrl: true
}
