meta {
  name: Create business with branch
  type: http
  seq: 1
}

post {
  url: {{baseURI}}core/businesses
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Addrex hotels",
    "country": "Nigeria"
  }
}

settings {
  encodeUrl: true
}
