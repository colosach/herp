meta {
  name: Create business
  type: http
  seq: 1
}

post {
  url: {{baseURI}}core/business
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Palmwinexpress hotels",
    "country": "Nigeria",
    "address_one": "30 omu road",
    "payment_type": ["pos"]
  }
}

settings {
  encodeUrl: true
}
