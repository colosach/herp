meta {
  name: Reset user password
  type: http
  seq: 1
}

post {
  url: {{baseURI}}admin/user/4/reset-password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "new_password": "password"
  }
}

settings {
  encodeUrl: true
}
