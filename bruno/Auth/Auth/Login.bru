meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{baseURI}}auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "admin",
    "password": "password1"
  }
}

tests {
  const data = res.body;
  bru.setEnvVar("token", data.token);
  
}

settings {
  encodeUrl: true
}
